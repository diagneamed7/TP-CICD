name: Test Runner
on:
  push:
    branches:
      - main
jobs:
  test-runner:
    runs-on: [self-hosted, label-1]
    steps:
      - name: Verification
        run: echo "Test de fonctionnement du Runner !"
      - name: Tests de commandes
        run: |
          echo "Runner OK"
          hostname
          whoami
          uname -a